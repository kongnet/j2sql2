{
  "name": "j2sql2",
  "version": "2.0.13",
  "description": "A Node.js database abstraction layer with unified API for MySQL, Redis, MSSQL, DMDB and RabbitMQ. Enhanced with CRUD operations, field validation, and camelCase conversion. | Node.js 数据库抽象层，支持 MySQL、Redis、MSSQL、达梦数据库和 RabbitMQ，提供增强的 CRUD 操作",
  "main": "index.js",
  "scripts": {
    "push": "npm version patch && npm publish . && git push",
    "cz": "git add . && git status && git cz && npm version patch && npm publish .",
    "test": "mocha --reporter spec --timeout 100000 --recursive tests/",
    "coveralls": "istanbul cover _mocha -- -r co-mocha --reporter spec --timeout 100000 --recursive tests/ && cat ./coverage/lcov.info | coveralls && rm -rf ./coverage"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/kongnet/j2sql2.git"
  },
  "author": "Kongnet <9601698@qq.com>",
  "license": "MIT",
  "dependencies": {
    "amqp-connection-manager": "4.1.10",
    "amqplib": "^0.8.0",
    "bluebird": "^3.7.1",
    "dmdb": "^1.0.43524",
    "ioredis": "^4.14.1",
    "meeko": "^1.8.298",
    "mssql": "^12.0.0",
    "mysql2": "^3.15.3",
    "promise-mysql": "^4.1.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-jt"
    }
  },
  "devDependencies": {
    "cz-jt": "*"
  }
}
